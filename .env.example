# GitHub personal access token (repo scope) for HTTPS remotes.
# Prefer SSH remotes when possible — leave this unset.
GITHUB_TOKEN=

# Optional override for config path
# GITCOMMIT_CONFIG=~/.gitcommit-bot/config.yaml
